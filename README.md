
  # Desktop POS System Design (Copy)

  This is a code bundle for Desktop POS System Design (Copy). The original project is available at https://www.figma.com/design/cgJvUmTDUZpEZ1fR7LU0pQ/Desktop-POS-System-Design--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  